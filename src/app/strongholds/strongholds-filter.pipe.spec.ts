import { StrongholdsFilterPipe } from './strongholds-filter.pipe';

describe('StrongholdsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StrongholdsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
